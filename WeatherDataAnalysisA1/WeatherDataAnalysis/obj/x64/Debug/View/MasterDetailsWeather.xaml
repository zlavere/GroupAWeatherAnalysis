<Page
    x:Class="WeatherDataAnalysis.View.MasterDetailsWeather"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:WeatherDataAnalysis.ViewModel"

    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <viewModel:DetailViewModel />
        <!-- ReSharper unused field error, but Bindings use this Data Context-->
    </Page.DataContext>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>


        <ListView x:ConnectionId='2' Grid.Column="0" x:Name="weatherInfoMaster" ItemsSource="{Binding WeatherInfoMaster, Mode=TwoWay}"
                  SelectedItem="{Binding SelectedWeatherInfoDetail, Mode=TwoWay}" HorizontalAlignment="Left"
                  Height="Auto" Margin="0" VerticalAlignment="Top">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <TextBlock HorizontalAlignment="Stretch" Grid.Row="0"
                                       Text="{Binding Date, Converter={StaticResource OrdinalDateConverter}, Mode=TwoWay}" />
                            <TextBlock Grid.Row="1" Text="{Binding HighTemp, Mode=TwoWay}" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Grid Grid.Column="1" HorizontalAlignment="Center"
              Margin="0" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" Text="Date" HorizontalAlignment="Left" Margin="10,0,0,0"/>
            <DatePicker Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2"
                       Date="{Binding SelectedWeatherInfoDetail.Date, Mode=TwoWay, Converter={StaticResource DatePickerConverter}}" Margin="10,0,0,0" HorizontalAlignment="Left" />
            <!-- TODO Make TwoWay on Edit button-->
            <TextBlock Text="High Temperature" Grid.Column="0" Grid.Row="2" HorizontalAlignment="Left" Margin="10,0,0,0" />
            <TextBox Grid.Row="3" Grid.Column="0" Text="{Binding SelectedWeatherInfoDetail.HighTemp, Mode=TwoWay, Converter={StaticResource StringToInt}}" Margin="10,0,0,0"  HorizontalAlignment="Left" />
            <TextBlock Text="Low Temperature" Grid.Column="1" Grid.Row="2" HorizontalAlignment="Left" Margin="10,0,0,0" />
            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding SelectedWeatherInfoDetail.LowTemp, Mode=TwoWay, Converter={StaticResource StringToInt}}" Margin="10,0,0,0"  HorizontalAlignment="Left" />
            <TextBlock Text="Precipitation" Grid.Column="0" Grid.Row="4" HorizontalAlignment="Left" Margin="10,0,0,0" />
            <TextBox Grid.Row="5" Grid.Column="0" Text="{Binding SelectedWeatherInfoDetail.Precipitation, Mode=TwoWay}" Margin="10,0,0,0"  HorizontalAlignment="Left" />
        </Grid>

    </Grid>
</Page>

