<Page
    x:Class="WeatherDataAnalysis.View.MasterDetailsWeather"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:WeatherDataAnalysis.ViewModel"

    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <viewModel:DetailViewModel />
        <!-- ReSharper unused field error, but Bindings use this Data Context-->
    </Page.DataContext>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>


        <ListView x:ConnectionId='2' Grid.Column="0" x:Name="weatherInfoMaster" ItemsSource="{Binding WeatherInfoMaster}"
                  SelectedItem="{Binding SelectedWeatherInfoDetail, Mode=TwoWay}" HorizontalAlignment="Left"
                  Height="Auto" Margin="0" VerticalAlignment="Top">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <TextBlock HorizontalAlignment="Stretch" Grid.Row="0"
                                       Text="{Binding Date}" />
                            <TextBlock Grid.Row="1" Text="{Binding HighTemp}" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Grid Grid.Column="1" HorizontalAlignment="Left"
              Margin="0" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Column="0" Grid.Row="0" Text="Date" />
            <DatePicker Grid.Column="0" Grid.Row="1"
                       Date="{Binding SelectedWeatherInfoDetail.Date, Converter={StaticResource DatePickerConverter}}" Margin="10" />
            <!-- TODO Make TwoWay on Edit button-->
            <TextBlock Text="High Temperature" Grid.Column="1" Grid.Row="0" />
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedWeatherInfoDetail.HighTemp}" Margin="10" />

        </Grid>

    </Grid>
</Page>

